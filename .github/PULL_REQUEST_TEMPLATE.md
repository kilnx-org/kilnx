## What

<!-- What does this PR do? One sentence. -->

## Why

<!-- Why is this change needed? Link an issue if applicable. -->

## Test plan

- [ ] `go test -race ./...` passes
- [ ] `go vet ./...` passes
- [ ] `gofmt -l .` returns nothing
- [ ] `kilnx check examples/blog.kilnx` passes
- [ ] `kilnx test examples/blog.kilnx` passes
